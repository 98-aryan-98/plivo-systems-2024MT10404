cat << 'EOF' > NOTES.md
Our system implements a dual-threaded real-time transport architecture featuring a thread-safe jitter buffer, interleaved XOR forward error correction (FEC), and a reactive NACK fallback mechanism. The sender groups packets into interleaved blocks of 4, generating lightweight XOR parity packets, while explicitly serializing all headers to avoid compiler padding issues. The receiver decouples network packet reception from playout using a high-precision system clock thread to meet strict delivery timelines. We recommend grading the project at a delay of 60ms, which safely accommodates baseline WSL virtualized network latency while maintaining real-time fidelity. Network overhead is strictly controlled and kept well below the 2.00x multiplier ceiling. The design breaks and experiences deadline misses if network jitter exceeds the pre-deadline NACK trigger window or if packet loss density exceeds the single-frame recovery capability of an active FEC parity block. Memory growth is bounded dynamically through automated sliding-window map pruning.
EOF
